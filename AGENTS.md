# AGENTS

이 저장소는 공개 YouTube URL에서 채널 영상 메타데이터와 공개 transcript를 수집하는 Python CLI를 관리한다.

## 세션 시작

1. 이 문서를 끝까지 읽는다.
2. `git status --short`와 최근 커밋을 확인한다.
3. `README.md`에서 현재 CLI, 입력·출력 형식과 API 사용 범위를 확인한다.
4. 현재 목표, 이번 변경, 하지 않을 작업과 사용자 판단이 필요한 지점을 짧게 보고한다.

## 정본과 경계

- 설치·실행·출력 스키마: `README.md`
- 실제 변경 이력과 복구 기준: Git
- 수집 결과의 음식 여부·성과 등급·교정·태깅: 외부 `shorts-script-benchmark-library`

이 프로젝트는 공개 데이터 수집만 담당한다. 벤치마크 판정이나 대본 교정을 수집기 안에서 수행하지 않는다.

## 작업 원칙

- 공개 데이터만 수집하며 YouTube Studio·Analytics 비공개 지표를 추측하지 않는다.
- API key, 쿠키, 로그인 세션과 `.env` 내용을 저장·출력·커밋하지 않는다.
- 실제 API 전체 수집은 작은 limit 샘플 검증 후 사용한다.
- transcript 누락과 차단은 성공으로 꾸미지 않고 상태와 오류 근거를 보존한다.
- `artifacts/`, `output/`과 기타 실행 산출물은 사용자 자료로 보고 명시적 요청 없이 삭제·이동·스테이징하지 않는다.
- 사용자가 명시하지 않으면 서브에이전트나 불필요한 대량 네트워크 수집을 실행하지 않는다.

## 세션 종료

- 변경 범위에 맞춰 `make test` 또는 해당 CLI의 mock·fixture 검증을 실행한다.
- 명령·출력 스키마가 바뀌면 `README.md`를 갱신한다.
- 의도한 파일만 선택적으로 스테이징하고 Git 커밋한다.
- 수정 파일, 검증 결과, API quota 사용 여부와 사용자 확인 사항을 보고한다.

같은 파일을 여러 세션이 동시에 수정하지 않는다. 동시 작업은 별도 `codex/` 브랜치나 worktree로 격리한다.
